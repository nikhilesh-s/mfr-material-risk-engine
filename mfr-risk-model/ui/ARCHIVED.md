# Archived UI Workspace

This directory is a legacy frontend workspace retained for historical reference.

Deployment targets:
- Backend: repository root (`api/main.py`)
- Frontend: `frontend/`

Do not use this directory as a Vercel production root.
